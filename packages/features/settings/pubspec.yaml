name: settings_feature
description: Application settings feature module.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  core_kit:
    path: ../../core_kit
  data_kit:
    path: ../../data_kit

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift: 2.15.0
